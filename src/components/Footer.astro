<!-- href="https://tbot.xyz/lumber/"
         -->
<div
  id="popUpBg"
  class="fixed z-10 inset-0 bg-black/40 backdrop-blur-sm popup-disable flex justify-center items-center"
>
  a
  <section class="h-[95vh] w-[80vw] bg-white relative">
    <header class="absolute -top-2 -right-2">
      <img
        class="w-10 shadow-lg cursor-pointer"
        id="close-btn"
        src="/icons/close.png"
        alt="close"
      />
    </header>

    <iframe
      src="https://tbot.xyz/lumber/"
      title="TBot"
      width="100%"
      height="100%"></iframe>
  </section>
</div>

<footer class="bg-black mt-20">
  <div
    class="py-8 flex flex-col-reverse gap-6 md:gap-0 md:flex-row items-center"
  >
    <div class="flex-1"></div>
    <p class="text-white text-xs max-w-[50ch] text-center">
      Gracias por visitar mi espacio. ¡Espero que encuentres algo que resuene
      contigo!
    </p>
    <aside class="flex flex-1 justify-end items-center mr-10">
      <button
        id="btn-popup"
        class="bg-purple flex rounded-xl items-center px-4 py-1 pr-1"
      >
        <p class="text-sm">Mi juego<br /> favorito</p>
        <img src="/icons/play.png" alt="play" />
      </button>
    </aside>
  </div>
</footer>

<style>
  .popup-disable {
    display: none;
  }
</style>

<script>
  const btnPopup = document.querySelector("#btn-popup");
  const popupBg = document.querySelector("#popUpBg");
  const closeBtn = document.querySelector("#close-btn");

  closeBtn?.addEventListener("click", () => {
    popupBg?.classList.toggle("popup-disable");
  });

  btnPopup?.addEventListener("click", () => {
    popupBg?.classList.toggle("popup-disable");
  });
</script>
